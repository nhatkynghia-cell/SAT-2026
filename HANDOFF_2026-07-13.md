# 📋 BÀN GIAO PHIÊN 2026-07-13 → PHIÊN SAU (chuẩn bị DEPLOY chạy thử)

## 🎯 MỤC TIÊU PHIÊN SAU
Chuẩn bị + thực hiện **deploy chạy thử app lên Vercel prod** (https://sat-2026.vercel.app). Rà pre-deploy, deploy, smoke test luồng chính.

## ✅ TRẠNG THÁI HIỆN TẠI (đầu phiên sau)
- **Git:** origin/main = `8e4af6f` (ĐỒNG BỘ, ahead/behind 0/0). Push đã hoạt động lại.
- **Credential:** `~/.gitcreds-sat2026` đã lưu token fine-grained mới (Contents R/W). Helper trỏ đúng `/c/Users/Admin/`. Push tự động OK. (Máy Windows, user `Admin`, ổ F:.)
  - ⚠️ Push over HTTPS bị treo với HTTP/2 → đã set `http.version HTTP/1.1` + `http.postBuffer 500MB` trong repo. Nếu push treo lại: chạy nền (`run_in_background`) vì có credential helper phụ hỏi device-code (bỏ qua được, push vẫn exit 0).
- **Baseline XANH:** tsc sạch · test 489/489 · lint 0 · build 78 pages. Node ở `/c/Program Files/nodejs` (export PATH trước).
- **Môi trường sau chuyển máy:** ĐÃ sửa quyền NTFS (takeown/icacls) + xóa 1570 file rác `(1)`. Nếu gặp lại lỗi này xem memory `machine-transfer-acl`.

## ✅ PHIÊN NÀY (2026-07-13) ĐÃ LÀM
1. Sửa môi trường sau chuyển máy sang ổ F: (ACL + rác `(1)` + git index) — chi tiết memory.
2. Redesign login/đăng ký theo brand (tabs, xác nhận mật khẩu, hiện/ẩn, quên mật khẩu, full-bleed qua `AppShell`).
3. OAuth Google/FB scaffold: nút + `/auth/callback` route + middleware public `/auth`; nút TỰ ẩn khi provider chưa bật (query `/auth/v1/settings`).
4. Polish 19 trang: CTA off-brand (xanh/indigo/sky/tím) → gold gradient; a11y (contrast text-amber-950, aria-label/htmlFor, flashcard keyboard); fix 3 bug missing-state (vocab báo nhầm "hoàn thành" khi lỗi mạng, pvp kẹt disable, tower im lặng thoát).
5. Bàn giao: `TODO_USER.md` + `OAUTH_SETUP.md` + file này.

## 📋 VIỆC PHIÊN SAU — theo thứ tự

### A. Đầu phiên (luôn làm)
1. `export PATH="$PATH:/c/Program Files/nodejs"` rồi verify: tsc + test + lint + build trong `sat-prep-web/`. Kỳ vọng 489 test / build 78 pages.
2. Đọc `TODO_USER.md` → HỎI user đã làm nhóm nào (rotate secret? creds VNPay/MoMo? OAuth?) rồi tick.

### B. Pre-deploy checklist (Claude tự rà — reversible)
3. **Env parity:** so `.env.local` (7 key: SUPABASE_URL/ANON/SERVICE_ROLE, OPENAI_API_KEY/MODEL, SAT_PREP_SECRET, ADMIN_SECRET) với env trên Vercel. Xác nhận `NEXT_PUBLIC_*` = plain, còn lại = sensitive. (Bug đã biết: NEXT_PUBLIC_* để sensitive sẽ vỡ deploy.)
4. **vercel.json** (`sat-prep-web/vercel.json`) — đọc, xác nhận cấu hình build/region đúng.
5. **Secret không lọt git:** `git ls-files | grep -iE 'env.local|.anv'` phải rỗng (đều gitignored). Xác nhận `.anv` (chứa OPENAI key) KHÔNG bị track.
6. **Rà rate-limit + RLS** các route tiền (đã có từ trước — chỉ xác nhận không regression sau polish).
7. Nếu user ĐÃ cấp creds VNPay/MoMo → nạp env → verify roundtrip live + field-order chữ ký IPN MoMo.

### C. Deploy
8. Kiểm Vercel token còn sống (`~/.vercel-token` đã MẤT khi chuyển máy — HỎI user cấp lại, hoặc user tự deploy qua dashboard/git-push-trigger).
9. Deploy: nếu Vercel nối GitHub thì push main đã auto-deploy; nếu không, `vercel --prod` (cần token). Xác nhận build Vercel xanh.
10. **Smoke test prod:** login (email), diagnostic, 1 luồng grade/economy, /upgrade render giá đúng. Dùng script `supabase_smoketest.mjs` (ở thư mục cha) nếu hợp.

### D. Rủi ro cần chú ý
- Team Vercel: `sat-2027` | project: `sat-2026` | app prod: https://sat-2026.vercel.app | Repo: github.com/nhatkynghia-cell/SAT-2026
- Account test: truongsonht.xd@gmail.com (UUID c43f015e-...) — pass do user giữ (docs ghi `Nghia@123` có thể đã đổi → HỎI).
- DB direct: `postgresql://postgres:<pw>@db.yynszcfqcvbnuvguwtfy.supabase.co:5432/postgres` (pw trong .env, KHÔNG ghi git). Cài pg: `npm i pg --no-save --legacy-peer-deps` (script đặt TRONG sat-prep-web/).
- Next.js 16 + Turbopack, `proxy.ts` thay middleware. Đọc `node_modules/next/dist/docs/` khi nghi API.
- Dev server hay tự chết giữa phiên → preview_list kiểm rồi preview_start lại. Screenshot hay timeout do animation infinite → dùng preview_inspect/eval thay thế.
