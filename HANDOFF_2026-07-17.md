# 📋 BÀN GIAO PHIÊN 2026-07-17 → PHIÊN SAU

## 🎯 PHIÊN NÀY ĐÃ LÀM
1. **Rotate OpenAI key** (07-14): wire prod Vercel (Production+Preview, Sensitive) + redeploy + verify sống.
2. **Thay VNPay/MoMo → Stripe** (Checkout + webhook): xem [[stripe-payment-migration]]. Disable vnpay/momo giữ nguyên code, VND zero-decimal tái dùng `confirm_payment`. Verify tsc/lint/test 498/build 79.
3. **Sự cố ổ F: bad sector** (EIO) làm `next build` gãy → chuyển toàn bộ sang ổ C:. Xem [[f-drive-eio-corruption]].
4. **Commit local** `ed982f5` nhánh `feat/stripe-payment-migration` (CHƯA push).
5. **Audit toàn dự án** → 33 việc tồn (workflow 5 agent).

## ✅ TRẠNG THÁI ĐẦU PHIÊN SAU
- **BẢN LÀM VIỆC = ổ C:** `C:/Users/DELL/Desktop/SAT_Rescue 17.7/SAT_Rescue/0.SAT.Guru 2026/10.SAT_Prep_App 30.6/10.SAT_Prep_App 30.6/10.SAT_Prep_App/10.SAT_Prep_App/sat-prep-web`. KHÔNG dùng F: (bad sector, node_modules/next EIO).
- **Git:** nhánh `feat/stripe-payment-migration` HEAD `ed982f5` (Stripe), trước đó `da0b370`=origin/main. **Remote CHỈ có main → nhánh Stripe chỉ tồn tại local, CHƯA push.**
- **Prod:** https://sat-2026.vercel.app đang chạy `da0b370` (BẢN CŨ, chưa có Stripe). Đẩy Stripe lên prod = push nhánh (Vercel auto-deploy).
- **Baseline C: XANH:** tsc sạch · lint 0 · test 498/498 · build 79 pages. Node `/c/Program Files/nodejs`.

## 🖥️ LỆNH TIẾP TỤC (chép mỗi phiên bash mới)
```bash
export PATH="$PATH:/c/Program Files/nodejs"
cd "/c/SAT_Rescue/0.SAT.Guru 2026/10.SAT_Prep_App 30.6/10.SAT_Prep_App 30.6/10.SAT_Prep_App/10.SAT_Prep_App/sat-prep-web"
npx tsc --noEmit ; npm run lint ; npm test ; npm run build
```
⚠️ Bash tool hay nuốt stdout (echo/cat không in dù exit 0) → ghi kết quả ra FILE trong repo rồi Read. KHÔNG dùng /tmp (cwd lệch).
⚠️ Preview/dev-server browser: session neo F: nên preview_start hỏng (cache đường F:). Mở Claude MỚI với thư mục gốc C: mới chạy được preview.

---

## 📋 33 VIỆC TỒN (xếp theo ưu tiên)

### 🔴 CAO — đưa Stripe lên prod
1. **Push nhánh Stripe** `ed982f5` → GitHub (backup + auto-deploy). [cả hai]
2. **Cấp Stripe keys + webhook prod** — `.env.local` chưa có `STRIPE_SECRET_KEY`/`WEBHOOK_SECRET`/`PUBLISHABLE_KEY` → `/api/payment/create` 503. User lấy `sk_test_`/`pk_test_` + tạo webhook `checkout.session.completed` → `whsec_`. Claude verify card 4242. [cả hai]
3. **migration_stripe_gateway.sql** trên Supabase (mở CHECK gateway; chưa chạy → 23514) + kiểm `user_progress.sql`. [user]
4. **Sync env server-only sang Vercel** (SERVICE_ROLE/OPENAI/SAT_PREP_SECRET/ADMIN_SECRET/STRIPE_*/APP_BASE_URL; VERCEL_OIDC_TOKEN KHÔNG copy). [user]

### 🔴 CAO — bảo mật secret lộ
5. **Rotate SAT_PREP_SECRET** (ảnh 06-29) + re-ký streak cũ (HMAC). [cả hai]
6. **Xóa GitHub PAT cũ** `ghp_...HETIG`. [user]
7. **Xóa Vercel token cũ** `vcp_...`. [user, TB]

### 🔴 CAO — hạ tầng
8. **chkdsk F: /f /r** sửa bad sector. [user]

### 🔴 CAO — tính năng (Claude làm được)
9. **Nhóm A** (QUYẾT 07-07): switch model AI theo tier ở `chat/route.ts`+`generate-practice/route.ts` + PRICING `ai-cost.ts`; gỡ 3 dòng A2/A3/A4 `upgrade/page.tsx:34-36`. Ultimate hiện vẫn gpt-4o-mini. [claude]

### 🟡 TRUNG BÌNH — hạ tầng
10. Migration cột `pvp_*` (nếu mở PvP; `economy-store.ts:286-319`). [user]
11. Verify cron `settle-speed-quiz` có bảo vệ + bảng speed_quiz. [cả hai]
12. **OAuth Google** (`OAUTH_SETUP.md`): Client + Supabase Provider + Redirect URLs. [user]

### 🟡 TRUNG BÌNH — conversion/tính năng
13. **Nhóm B**: gate Free tầng API `generate-practice/route.ts:106` (moduleType ∈ FREE_DOMAINS). [cả hai]
14. **C4**: mất dữ liệu diagnostic khách login muộn (write guest FAIL 22P02) — chốt ép login. [cả hai]
15. **Cụm CTA C5–C8** (pure-UI): skill-tree unlock / real-exams redirect / AI quota "còn N" / empty dashboard CTA. [claude]
16. **Affiliate 35%** (KOL 25%+giảm 10%): bảng + discount create + hoa hồng webhook + admin + ô coupon. [claude]
17. **Giải Top-5 toàn quốc**: nghiên cứu chống gian lận + bảng leaderboard cross-user. [claude]

### 🟡 TRUNG BÌNH — beta
18. Pháp lý điều khoản trẻ vị thành niên. [user]
19. Kênh tuyển 100 beta. [user]

### 🟡 TRUNG BÌNH — verify (có thể lỗi thời)
20. Verify mastery tracking luồng luyện THƯỜNG (ngoài diagnostic) ghi đúng skill. `master_task_list.md:38 [~]`. [claude]
21. Verify anti-cheat economy cutover — client còn tính coins/xp/level? `GamificationContext.tsx`. `master_task_list.md:28 [~]`. [claude]

### 🟡 TRUNG BÌNH — git/repo
22. Dọn nhánh rác (lane-a/b/c WIP 'rescued', main(1), trial/smoke) — cherry-pick WIP thật, `-D` destructive cần duyệt. [cả hai]
23. Thống nhất bản C:/F: sau chkdsk. [cả hai]

### 🟢 THẤP
24. Chốt giá 4 gói cuối. [user]
25. OAuth Facebook. [user]
26. Bật lại VNPay/MoMo (cần pháp nhân + verify field-order chữ ký IPN MoMo). [cả hai]
27. Wave 2: cap PvP/tower theo tier (RPC tryConsumePvpFightAtomic). [claude]
28. Gỡ route `/api/migrate-data` + file JSON legacy trước prod (E2). [claude]
29. Dọn comment lỗi thời `subscription.ts:8` + `auth.ts:8-13` (E1). [claude]
30. ROOT E #3: `p_user_id` tường minh defense-in-depth. [claude]
31. Verify skill-tree/base-stats UI + Boss adaptive `master_task_list.md:40-42 [~]`. [claude]
32. Click-verify UI `/admin/redemptions`. [claude]
33. Roadmap Phase 3/4 (mobile/social/B2B/mở rộng ACT-IELTS). [cả hai]

---

## ⚠️ LƯU Ý PHIÊN SAU
- Agent audit từng báo NHẦM "commit ed982f5 không tồn tại" — đã verify trực tiếp là CÓ. Luôn kiểm git state trực tiếp, đừng tin báo cáo agent về git.
- Next.js 16 + Turbopack, `proxy.ts` thay middleware. Test = `node:test` thuần (KHÔNG vitest/jest).
- Tài khoản test: `truongsonht.xd@gmail.com` (pass user giữ — docs ghi `Nghia@123` có thể đã đổi).
- Việc CAO đáng làm ngay không cần chờ user: **#9 Nhóm A** (reversible, đã có quyết định).
